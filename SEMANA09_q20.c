/*#include <stdio.h>

int main(){
  int vetor[10];
  int i , x, y, aux;
  for(i=0;i<10;i++){
    scanf("%i",&vetor[i]);
  }
  for(x=0;x<10;x++){
    for(y=x;y<10;y++){
      if(vetor[x]>vetor[y]){
        aux=vetor[x];
        vetor[x]=vetor[y];
        vetor[y]=aux;
      }
    }
  }
  for(i=0;i<10;i++){
    printf("%i ",vetor[i]);
  }
  printf("\n");
  for(i=9;i>=0;i--){
    printf("%i ",vetor[i]);
  }

  return 0;
}
*/
